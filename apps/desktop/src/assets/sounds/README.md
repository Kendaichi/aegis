# Chat notification sounds

Replace these files with your own short MP3 clips (keep the same filenames so imports in `Chat.tsx` stay valid):

- `send.mp3` — played when you send a message
- `receive.mp3` — played when the assistant (or an error reply) appears

The committed files are minimal silent clips generated for the build; swap them for audible tones as you like.
