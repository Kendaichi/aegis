# Compose-level env — copy to .env and fill in
SUPABASE_URL=
SUPABASE_KEY=

# real calls Ollama; mock returns canned responses (no model download required)
VLM_MODE=mock
VLM_MODEL=gemma3:4b
FRAME_INTERVAL_SECONDS=2
